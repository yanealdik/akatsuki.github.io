name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]  # Или master, в зависимости от названия вашей основной ветки

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '22s'
          
          - name: Install and Build 🔧
          run: |s
            npm ci
            npm install --save-dev node-polyfill-webpack-plugin crypto-browserify
            npm run build
          
      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: dist  # Папка Vite по умолчанию
          branch: gh-pages  # Ветка для деплоя